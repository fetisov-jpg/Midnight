---
description: A description of your rule
---

1. Все функции должны содержать docstring в формате Google.
2. Использовать `typing` аннотации для всех аргументов.
3. Максимальная длина строки — 88 символов (как в Black).
4. Все публичные методы должны быть задокументированы.
5. При рефакторинге — сохранять обратную совместимость.

## Архитектурные принципы
- Использовать паттерн Repository для доступа к данным
- Внедрять зависимости через конструктор, а не через глобальные переменные
- Логирование везде, где есть внешние вызовы